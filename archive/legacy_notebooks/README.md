# Archived legacy notebooks

`WSDC_Points_parser_API_optimized_local.ipynb` — superseded by `parser/` + `scripts/cloud_parse.py` + `transform/preprocess_with_log.py`.

Do not extend; reference only for historical WSDC parse logic.
