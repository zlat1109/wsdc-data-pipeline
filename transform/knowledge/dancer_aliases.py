"""Known alternate dancer display names -> WSDC dancer_id.

Seeded into core.dancer_aliases; does not rewrite SCD2 history automatically.
"""

from __future__ import annotations

# alias (exact display string) -> dancer_id
DANCER_ALIAS_TO_ID: dict[str, int] = {
    # Add known maiden/married or typo pairs as discovered during audits.
}
